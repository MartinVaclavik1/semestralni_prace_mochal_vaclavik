<UserControl x:Class="semestralni_prace_mochal_vaclavik.Views.LogovaciTabulkaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:semestralni_prace_mochal_vaclavik.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadCommand}" PassEventArgsToCommand="True"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    
    <Grid Margin="20">
        <DataGrid
          ItemsSource="{Binding LogovaciTabulka}" 
          IsReadOnly="True" 
          AutoGenerateColumns="True" CanUserAddRows="false" x:Name="logovaciTabulkaGrid" d:ItemsSource="{d:SampleData ItemCount=5}"/>

    </Grid>
</UserControl>
