<UserControl x:Class="semestralni_prace_mochal_vaclavik.Views.OkrskyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:semestralni_prace_mochal_vaclavik.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="850">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="429*"/>
            <ColumnDefinition Width="365*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <!-- Nadpis -->
        <Label x:Name="OkrskyNazev" Content="Přehled Okrsků" Margin="10,5,0,5" FontSize="20" FontWeight="Bold" Grid.ColumnSpan="2"/>


        <!-- DataGrid -->
        <DataGrid x:Name="OkrskyGrid" 
       d:ItemsSource="{d:SampleData ItemCount=5}" 
       ItemsSource="{Binding Okrsky}"
       Margin="10,10,10,10"
       SelectionMode="Single"
       SelectionUnit="Cell"
       IsManipulationEnabled="True"
       AutoGenerateColumns="False" Grid.Row="1"
               CanUserAddRows="false" Grid.ColumnSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Okrsku" Binding="{Binding Id}" Width="*" Visibility="Collapsed"/>
                <DataGridTextColumn Header="Název Okrsku" Binding="{Binding Nazev}" Width="*"/>
                <DataGridTemplateColumn Header="" Width="SizeToCells">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Upravit" 
                                 Command="{Binding DataContext.UpravitOkrsekCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                 CommandParameter="{Binding}" 
                                 Margin="1,1" >
                            </Button>
                                 <!--Visibility="{Binding DataContext.AdminControlsVisible, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>-->
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="" Width="SizeToCells">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Odebrat" 
                                 Command="{Binding DataContext.OdebratOkrsekCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                 CommandParameter="{Binding}" 
                                 Margin="1,1">
                            </Button>
                                 <!--="{Binding DataContext.AdminControlsVisible, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>-->
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="2" Margin="10,10,10,10" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="155*"/>
                <ColumnDefinition Width="141*"/>
                <ColumnDefinition Width="478*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Grid.Column="1"
                    Grid.Row="1"
                    Content="Přidat"
                    Command="{Binding PridatOkrsekCommand}" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Top" 
                    Width="109" Height="20" 
                    Margin="0,5,0,0"/>
            <Label Content="Název Okrsku" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0" Height="26" Width="120"/>
            <TextBox Text="{Binding NovyNazevOkrsku, UpdateSourceTrigger=PropertyChanged}" x:Name="pridatOkrsekNazev" HorizontalAlignment="Center" Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Center" Width="135" Height="18"/>
        </Grid>

    </Grid>
</UserControl>
